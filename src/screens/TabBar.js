import React, { Component } from 'react';
import {
   StyleSheet,
   SafeAreaView, 
   Text, 
   View,
  TextInput,
    TouchableOpacity } 
   from 'react-native';
   

   export default class TabBar extends React.Component {
    render() {
      return (

        <Text/>
      );
    }
  }

